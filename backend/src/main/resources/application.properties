# Application
spring.application.name=music-catalog-insights
server.port=${PORT:8080}

# Database (Permanent Supabase PostgreSQL - IPv4 Pooler)
spring.datasource.url=jdbc:postgresql://aws-1-ap-northeast-2.pooler.supabase.com:6543/postgres?sslmode=require&prepareThreshold=0
spring.datasource.driverClassName=org.postgresql.Driver
spring.datasource.username=postgres.ucuyqagkzwgmahymclog
spring.datasource.password=SonicVault2026@

# JPA
spring.jpa.database-platform=${DB_DIALECT:org.hibernate.dialect.PostgreSQLDialect}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# 100k CCU Scalability Optimization
server.tomcat.threads.max=1000
server.tomcat.accept-count=10000
spring.datasource.hikari.maximum-pool-size=100
spring.datasource.hikari.minimum-idle=10
spring.datasource.hikari.connection-timeout=30000

# JWT Configuration
jwt.secret=MusicCatalogInsightsPlatformSuperSecretKeyForJWTTokenGeneration2024SecureKey
jwt.expiration=86400000

# iTunes API
itunes.api.base-url=https://itunes.apple.com

# Cache
spring.cache.type=simple

# CORS
app.cors.allowed-origins=http://localhost:5173,http://localhost:3000

# Logging
logging.level.com.musicinsights=INFO
